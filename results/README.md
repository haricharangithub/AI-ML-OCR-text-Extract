Use this directory to store generated OCR outputs and accuracy metrics.

Generate JSON outputs for the bundled images:
```
python -m src.text_extraction --images "ReverseWay Bill" --output results/outputs.json
```

Add OCR screenshots for each test image here as well (e.g., `screenshots/<image>.png`).

